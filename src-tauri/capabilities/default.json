{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default desktop capability for invoke and event usage.",
  "windows": ["main"],
  "permissions": ["core:default"]
}

